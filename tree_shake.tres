[gd_resource type="ShaderMaterial" load_steps=2 format=3 uid="uid://c4v4rdbr4bjxa"]

[sub_resource type="Shader" id="Shader_acpv2"]
resource_local_to_scene = true
code = "shader_type canvas_item;

// 摇晃强度
uniform float shake_intensity = 0.0;

// 摇晃速度
uniform float shake_speed = 20.0;

void vertex() {
	vec2 shake = vec2(0.0);
	if(VERTEX.y < 0.0){
		shake.x = sin(TIME * shake_speed + VERTEX.y) * shake_intensity;
	}
	VERTEX.xy += shake;
}
"

[resource]
resource_local_to_scene = true
shader = SubResource("Shader_acpv2")
shader_parameter/shake_intensity = 0.0
shader_parameter/shake_speed = 20.0
